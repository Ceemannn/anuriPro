# Email Configuration (SMTP)
# You can use services like Gmail, SendGrid, Mailgun, or your own SMTP server

# SMTP Host (e.g., smtp.gmail.com, smtp.sendgrid.net)
SMTP_HOST=smtp.gmail.com

# SMTP Port (usually 587 for TLS, 465 for SSL)
SMTP_PORT=587

# Use secure connection (true for port 465, false for port 587)
SMTP_SECURE=false

# SMTP Username (your email address or API username)
SMTP_USER=your-email@gmail.com

# SMTP Password (app password for Gmail, API key for services like SendGrid)
SMTP_PASSWORD=your-app-password

# From email address (optional, defaults to SMTP_USER)
SMTP_FROM=noreply@anurigroup.com
